// The standalone admin apply page was deprecated: the admin apply-payment UI
// is now integrated into `components/admin-dashboard.tsx` and exposed via the
// "Agregar Pago Manual" modal. This file remains as a no-op to avoid
// accidental external links; all admin UI changes should be made in
// `components/admin-dashboard.tsx` per project convention.

export default function Page() {
  return null
}
